# kcore X.Y.Z

## Highlights

- 

## Upgrade / install

- **ISO**: flash `kcoreos-X.Y.Z-x86_64-linux.iso` (verify `SHA256SUMS`).
- **kctl**: extract `kctl-X.Y.Z-linux-x86_64.tar.gz` and run `./kctl --help` (glibc Linux x86_64, built via this repo’s Nix flake).

## Checksums

See `SHA256SUMS` attached to the GitHub Release.
